# Web Framework
fastapi==0.115.5
uvicorn==0.32.1

# Configuration
pydantic-settings==2.6.1
python-dotenv==1.0.1

# Database
sqlalchemy==2.0.36
alembic==1.14.0
asyncpg==0.30.0
psycopg2-binary==2.9.10
# MySQL async driver — used ONLY to read the ATS module's MySQL db
# (ATS_DATABASE_URL). Our own database stays Postgres.
aiomysql==0.3.2

# Cache
redis==5.2.0

# AWS S3
boto3==1.35.74

# HTTP Client (for calling ATS REST APIs)
httpx==0.28.0

# Auth
python-jose==3.3.0

# AI
anthropic
langgraph

# LiveKit - Voice Pipeline (1.x)
livekit-agents[anthropic,deepgram,elevenlabs,silero]

# Simli - Real-time avatar
simli-ai

# Scheduler (Phase 8 — workflow durability)
apscheduler>=3.10.0

# Phase 9 — Hardening
slowapi>=0.1.9           # Rate limiting (FastAPI-native, wraps limits library)
python-json-logger>=2.0.7  # Structured JSON logging

# Phase 13 — Voice & Delivery Analysis (post-interview acoustic scoring)
praat-parselmouth>=0.4.7  # Praat acoustics: pitch, jitter, shimmer, HNR
librosa>=0.10.0           # Loudness/energy dynamics, speech/silence split
soundfile>=0.12.0         # WAV I/O backend for librosa
